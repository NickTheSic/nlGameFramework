#include "whalepass_gg.h"
#include "nl_lib.h"

void wgg_authenticate(const char* publish_key, const char* game_id, const char* player_id)
{
    NL_UNUSED(publish_key);
    NL_UNUSED(game_id);
    NL_UNUSED(player_id);
}
