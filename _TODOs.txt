
Should start with making a game or a few to see how it feels to use before I settle on anything specific

Should update the input in some way so I can have all controls together; Keyboard, mouse and controller should have some common interface?

In Progress: Remaking a renderer so I have a refresher of OpenGL


Next Steps:

Cleanup projects!  I want fewer projects to look through while I try to get this going again
    - this could include make a better way to build the projects so I can hide important test projects inside of a file

Consolodate UI Projects into one
    - Batch Render Images
    - Batch Text
    - Anchor Points
    - Can update the UI as I need in the future
        - Dynamic Sized Boxes

Need text rendering for some basic UI editor style stuff I want to make for fun

Make reusable rendering object.  So I can batch render sprites and UI with some core code.
    - might also include a common camera setup too

Idea: UI Batching keeps track of the index into the buffer and I only change one portion of the buffer?
        - 3 Elements, if the second is the only one that changes than I only need to update the part of the buffer

Idea: Allocate 1 memory block per object and let other pointers index into the main block.  Reduces the amount of allocations!
