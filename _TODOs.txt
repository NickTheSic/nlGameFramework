
/////////////////////////////////////////////////////////////////
//                       In Progress                           //
/////////////////////////////////////////////////////////////////

Getting a renderer setup going on.  Hopefully it batches sprites, can draw lines and the endless grid
Polygon rendering with line loops would be nice but would be lots more draw calls
    - Draw calls don't matter at this stage of the program!
    - And it doesn't mater to add the first point last/double up line points .... Now I want to add indices again

Check if the OpenGL debug logging is working

/////////////////////////////////////////////////////////////////
//                   Needs to be done                          //
/////////////////////////////////////////////////////////////////

Might be nice to refactor my NL log. For different levels of error and verbosity.  Plus adding a tag of the class / file

Allocate more of the core program on the heap cus I think I might be running out of stack
    - Could also check the stack size too and confirm this theory

Work on a game to know if it feels right to use!
    - I need sprite rendering!

'Effecient' Sprite rendering
    - Batching together draw calls
    - puting images together in a spritesheet


One person called their renderer a Canvas
- It handled sprites and debug lines.  
- I like the name Canvas.  Kinda a global thing that you can draw to and could draw anything to
- Sounds like I could have a game Canvas, UI canvas.  To encapsulate the camera instead of a game and ui camera


Refactor / Cleanup / Reorganize

- My platform layer is still an abstraction layer, platform init calls all the other inits. I could make them the same or remove it
- Publish scripts require 7z and butler to push to itch
- This file and game ideas could be in sub folder
- Maybe look into building wintux and adding the exe icon?
- MinimalShell for emscripten is in scripts but it could be in res? 

/////////////////////////////////////////////////////////////////
//                     Things to Look Into!                    //
/////////////////////////////////////////////////////////////////

Open GL Depth Testing:
    - Can always draw my UI after my Game, and debug stuff in between

How important PixelFormatDescriptor is:
    - I have a 31 bit Depth, 1 bit stencil buffer.  I saw a 24 Bit depth, and 8 bit Alpha in a Cakez77 video

The importance of cleaning up the 'library' to remove old, deprecated or unused functionality
    rewrite main files?  
    - Do I consolodate down to 1 main function or keep it as is
    - If I keep it as is, what functions are important to the 'game'



/////////////////////////////////////////////////////////////////
//             Things to Consider Implementing                 //
/////////////////////////////////////////////////////////////////


Idea: Allocate 1 memory block per object and let other pointers index into the main block.  Reduces the amount of allocations!
        Use Case: Mesh Struct where Indices and Positions can be part of 1 object
        From the Jonothan Blow introduction for a new language where he shows this as an example saying @joint_memory would be a neat concept

struct Mesh
{
    void *memory; // Maybe a char*
    vector3 *positions; // points to memory
    unsigned int *indices; // points to memory after the positions

    int indice_count;
    int vertice_count;
};
With a bump/areana allocator I give it to memory so positions and indices are together?


Do I really want to do a dll and hot reloading?  I am firmly in the NO side right now since I am still figuring things out
-- DLL would allow for an "Editor" dll that could contain a FBO, or other render target, that the game runs inside of.
--- I do think it would be cool to have the:           Game
                                                APP <
                                                       Editor -> Game
                    - Having tested out the above in a separate project, it was interesting.  It felt like class/inheritence with extra steps.
                    - Would need a few defines to define away the editor code from the Game DLL 
                    - which doesn't matter because the root app wouldn't load the functions


Returning pointers instead of passing them in as objects?
- Specifically during allocation
- might require a larger rewrite
- Now lifetime objects are more heap than stack allocated?
-- Would be fine if I am using the arena style allocator


My current (March 2026) shader and renderer sorta hide opengl implementation details.
Since opengl functions are loaded at runtime, on windows, I could in theory rename them?
I would then have to do this for web which I just include the headers 
at this time I only use opengl so I don't need the abstraction exactly
Will learn more as I create more with this


Could have a RendererInit (RendererTypes) 
    - Exists all renderers but only creates the ones I plan to use
    - Get crazy with it and create a renderer when I call _draw_type()
        - While it could be interesting I wouldn't want it in the final game; just while testing. 
        - Even then there is a lot of generic things that could be taken out of a real game but for testing maybe it is good?
            - Need to know sizes before hand too

